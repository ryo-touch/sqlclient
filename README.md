# sqlclient

An interactive, read-only terminal client for MySQL and PostgreSQL.

Implementation is in progress. See `SPEC.md` for the complete specification.

```bash
bun install
bun run src/index.tsx
```
