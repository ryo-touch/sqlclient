# PostgreSQL services used only by parser tests
[analytics]
host = db.example.test
port = 5433
user = analyst
dbname = warehouse

; Values may contain equals signs
[local]
host=localhost
user=developer=name
